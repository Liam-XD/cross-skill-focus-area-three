{
  "name": "trello-api-tests",
  "version": "1.0.0",
  "description": "End-to-end testing project using Playwright, TypeScript & Cucumber.",
  "scripts": {
    "test:smoke": "npx cucumber-js --tags '@smoke'",
    "test:cucumber": "cucumber-js --require-module ts-node/register --require features/steps/**/*.ts features --format json:reports/cucumber-report.json",
    "report:cucumber": "node reports/generate-cucumber-report.js",
    "dryrun": "cucumber-js features/ --dry-run --config cucumber.js"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.5.0",
    "@playwright/test": "^1.43.0",
    "cucumber-html-reporter": "^7.2.0",
    "playwright-cucumber-steps": "^0.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0",
    "dotenv": "^17.2.3"
  },
  "dependencies": {
  }
}
